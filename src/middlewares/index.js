const error = require('./error');
const auth = require('./auth');
const admin = require('./admin');
const adminOrSeller = require('./adminOrSeller');
const swagger = require('./swagger');

module.exports = {
  error,
  auth,
  admin,
  adminOrSeller,
  swagger,
};
